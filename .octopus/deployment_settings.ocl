connectivity_policy {
    allow_deployments_to_no_targets = true
}

versioning_strategy {

    donor_package {
        package = "bamboo"
        step = "Push package to Atlassian Marketplace"
    }
}